-- Initial database setup if needed.
-- SQLAlchemy will handle table creation, but we can seed some data here.

-- Optional: enable extensions or set timezone
-- SET timezone = 'UTC';

-- No need to pre-create tables since SQLAlchemy's Base.metadata.create_all(bind=engine) will do it on startup.
